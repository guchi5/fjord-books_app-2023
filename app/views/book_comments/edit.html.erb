<h1><%= t('views.common.title_edit', name: BookComment.model_name.human.downcase) %></h1>

<%= render "form", book_comment: @book_comment, book: @book_comment.book %>

<br>

<div>
  <%= link_to t('views.common.back', name: i18n_pluralize(BookComment.model_name.human.downcase)), @book_comment.book %>
</div>
