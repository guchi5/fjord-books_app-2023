<h1><%= t('views.common.title_new', name: BookComment.model_name.human.downcase) %></h1>

<%= render "form", book_comment: @book_comment, book: @book %>

<br>

<div>
  <%= link_to t('views.common.back', name: BookComment.model_name.human.downcase), book_path(@book) %>
</div>
